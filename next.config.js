/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/next-js-review",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
};

module.exports = nextConfig;
